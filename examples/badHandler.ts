/**
 * This is an empty handler with no type checking and no validation, used to test the system's tolerance for that.
 * It is not an example of anything useful.
 */

const handler = {};

export default handler;
